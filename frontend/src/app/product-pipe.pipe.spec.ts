import { ProductPipe } from './product-pipe.pipe';

describe('ProductPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPipe();
    expect(pipe).toBeTruthy();
  });
});
